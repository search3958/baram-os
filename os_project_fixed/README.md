# Custom AArch64 UEFI OS

This is a minimal OS that boots via UEFI on ARM64 (Raspberry Pi 3/4 and QEMU).
It features mouse pointer drawing and keyboard input handling using Rust and the `uefi` crate.

## How to run
1. `chmod +x build.sh`
2. `./build.sh`
